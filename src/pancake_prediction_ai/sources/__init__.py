"""Read-only source normalization adapters.

These modules transform externally observed market/protocol payloads into
canonical EventRecord objects. They do not own credentials, signing authority,
or transaction broadcast capability.
"""
